import NewJobFrom from "../../../../components/NewJobFrom";

// metadata
export const metadata = {
  title: "Add New Job Devjobsbd - Developer Jobs in Bangladesh",
  description: "Add a new job to the job board.",
  keywords:
    "add, new, job, job board, job portal, job site, job listing, job posting, job vacancy, job opportunity, job opening, job application",
};

const page = () => {
  return <NewJobFrom />;
};

export default page;
